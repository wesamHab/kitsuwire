import type { MetadataRoute } from "next";
import { categories, getAllArticles } from "@/lib/articles";
export default function sitemap():MetadataRoute.Sitemap{const base="https://kitsuwire.com";return[{url:base,lastModified:new Date(),changeFrequency:"daily",priority:1},...categories.map(category=>({url:`${base}/category/${category}`,lastModified:new Date(),changeFrequency:"daily" as const,priority:.8})),...getAllArticles().map(article=>({url:`${base}/article/${article.slug}`,lastModified:new Date(article.updatedAt??article.publishedAt),changeFrequency:"weekly" as const,priority:article.featured?.9:.7}))]}
